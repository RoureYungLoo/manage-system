<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luruoyang.mapper.DeptMapper">
    <insert id="save">
        insert into tb_dept
        values (null, #{name}, #{createTime}, #{updateTime}, 1)
    </insert>
    <!--    <update id="updateById">-->
    <!--        update tb_dept-->
    <!--        <set>-->
    <!--            <if test="dept.name!=null">-->
    <!--                name = #{dept.name},-->
    <!--            </if>-->
    <!--            <if test="dept.status!=null">-->
    <!--                status = #{dept.status},-->
    <!--            </if>-->
    <!--        </set>-->
    <!--        <where>-->
    <!--            id = #{id}-->
    <!--        </where>-->
    <!--    </update>-->
    <update id="update" parameterType="com.luruoyang.model.pojo.Dept">
        update tb_dept
        <set>
            <if test="name!=null">
                name = #{name},
            </if>
            <if test="status!=null">
                status = #{status},
            </if>
            <if test="updateTime!=null">
                update_time = #{updateTime},
            </if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>
    <select id="findById" resultType="com.luruoyang.model.pojo.Dept" parameterType="java.lang.Long">
        select id, name, create_time, update_time, status
        from tb_dept
        where id = #{id}
    </select>
    <select id="findPage" resultType="com.luruoyang.model.pojo.Dept" parameterType="com.luruoyang.utils.PageDto">
        select id, name, create_time, update_time, status
        from tb_dept
        limit #{param1}, #{param2}
    </select>
    <select id="findQuery" resultType="com.luruoyang.model.pojo.Dept" parameterType="com.luruoyang.model.dto.DeptDto">
        select id, name, create_time, update_time, status
        from tb_dept
        <where>
            <if test="id!=null and id!=''">
                and id = #{id}
            </if>
            <if test="name!=null and name!=''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="startTime!=null and startTime!='' ">
                and create_time <![CDATA[ >= ]]> #{startTime}
            </if>
            <if test="endTime!=null and endTime!=''">
                and create_time <![CDATA[<= ]]> #{endTime}
            </if>
        </where>
    </select>
</mapper>
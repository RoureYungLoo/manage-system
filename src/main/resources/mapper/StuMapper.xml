<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luruoyang.mapper.StuMapper">

    <resultMap id="stuMap" type="com.luruoyang.model.pojo.Stu">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="no" column="no"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="idCard" column="id_card"/>
        <result property="isCollege" column="is_college"/>
        <result property="address" column="address"/>
        <result property="degree" column="degree"/>
        <result property="graduationDate" column="graduation_date"/>
        <result property="clazzId" column="clazz_id"/>
        <result property="violationCount" column="violation_count"/>
        <result property="violationScore" column="violation_score"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="clazzName" column="clazz_name"/>
    </resultMap>
    <insert id="save" parameterType="com.luruoyang.model.pojo.Stu">
        INSERT INTO tb_stu
        VALUES (null,
                #{name},
                #{no},
                #{gender},
                #{phone},
                #{idCard},
                #{isCollege},
                #{address},
                #{degree},
                #{graduationDate},
                #{clazzId},
                #{violationCount},
                #{violationScore},
                #{createTime},
                #{updateTime})
    </insert>
    <update id="updateById" parameterType="com.luruoyang.model.pojo.Stu">
        UPDATE tb_stu
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="no != null and no != ''">
                no = #{no},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="degree != null ">
                degree = #{degree},
            </if>
            <if test="idCard != null and idCard != ''">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null ">
                is_college = #{isCollege},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="graduationDate != null ">
                graduation_date = #{graduationDate},
            </if>
            <if test="violationCount != null ">
                violation_count = #{violationCount},
            </if>
            <if test="violationScore != null ">
                violation_Score = #{violationScore},
            </if>
            <if test="updateTime != null ">
                update_time = #{updateTime},
            </if>
            <if test="clazzId != null ">
                clazz_id = #{clazzId},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <update id="violation">
        update tb_stu
        set violation_score = #{violationScore} + violation_score,
            violation_count = violation_count + 1
        where id = #{id}
    </update>
    <select id="degreeStatistic" resultType="com.luruoyang.model.dto.StuDegreeDto">
        select
            case s.degree
                when 1 then '初中'
                when 2 then '高中'
                when 3 then '大专'
                when 4 then '本科'
                when 5 then '硕士'
                when 6 then '博士'
                end as 'name',
            count(s.degree) as 'value'
        from tb_stu s
        group by s.degree
    </select>
    <delete id="deleteBatch" parameterType="java.lang.Long">
        DELETE FROM tb_stu
        <where>
            <foreach collection="list" open="id IN (" close=")" separator="," item="stuId">
                #{stuId}
            </foreach>
        </where>
    </delete>
    <select id="findPage" resultType="com.luruoyang.model.pojo.Stu">
        select s.*, c.name as 'clazz_name'
        from tb_stu s
        left join tb_clazz c on s.clazz_id = c.id
        <where>
            <if test="name!=null and name!=''">
                s.name like concat('%',#{name},'%')
            </if>
            <if test="degree!=null">
                and s.degree = #{degree}
            </if>
            <if test="clazzId!=null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
    </select>
    <select id="findStuById" resultType="com.luruoyang.model.pojo.Stu" parameterType="java.lang.Long">
        SELECT id,
               name,
               no,
               gender,
               phone,
               id_card,
               is_college,
               address,
               degree,
               graduation_date,
               clazz_id,
               violation_count,
               violation_score,
               create_time,
               update_time
        FROM tb_stu
        where id = #{id}
    </select>
    <select id="groupByClazz" resultType="com.luruoyang.model.dto.StuClazzCountDto">
        select c.name as 'clazzName', count(s.clazz_id) as 'clazzCount'
        from tb_stu s
                 left join tb_clazz c on s.clazz_id = c.id
        group by s.clazz_id;
    </select>
</mapper>